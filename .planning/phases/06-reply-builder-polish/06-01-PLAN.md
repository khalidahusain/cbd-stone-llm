---
phase: 06-reply-builder-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config/features.yaml
  - backend/app/core/schema_loader.py
  - backend/tests/test_schema_loader.py
autonomous: true
requirements:
  - PLSH-01
  - PLSH-05

must_haves:
  truths:
    - "Every feature in features.yaml has a display_name, category, and follow_up_question field"
    - "SchemaLoader parses the three new fields and populates FeatureDefinition"
    - "Existing tests still pass with the extended schema"
  artifacts:
    - path: "backend/config/features.yaml"
      provides: "display_name, category, follow_up_question for all 13 features"
      contains: "display_name:"
    - path: "backend/app/core/schema_loader.py"
      provides: "FeatureDefinition with display_name, category, follow_up_question fields"
      contains: "display_name"
  key_links:
    - from: "backend/config/features.yaml"
      to: "backend/app/core/schema_loader.py"
      via: "SchemaLoader.load() parses new fields"
      pattern: "feat_data\\.get\\(\"display_name\""
---

<objective>
Extend the feature schema with human-readable metadata: display_name, category, and follow_up_question for all 13 features.

Purpose: Establish the single source of truth for all user-facing field names, category groupings, and follow-up question text (per D-01, D-03, D-07). Plan 02 will consume these fields in ReplyBuilder.
Output: Updated features.yaml with 3 new fields per feature, updated FeatureDefinition dataclass, updated SchemaLoader.load().
</objective>

<execution_context>
@/Users/khalidahusain/Library/Mobile Documents/com~apple~CloudDocs/Work/Hopkins/Programming Projects/CBD-stone-LLM/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khalidahusain/Library/Mobile Documents/com~apple~CloudDocs/Work/Hopkins/Programming Projects/CBD-stone-LLM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reply-builder-polish/06-CONTEXT.md
@.planning/phases/06-reply-builder-polish/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/app/core/schema_loader.py:
```python
@dataclass
class FeatureDefinition:
    name: str
    model_column: str
    type: str
    required: bool
    description: str
    encoding: dict[str, int] = field(default_factory=dict)
    valid_range: Optional[dict] = None
    allowed_values: Optional[list] = None
    unit: Optional[str] = None
    strongly_recommended: bool = False
    triggers_fast_path: bool = False
    inference_prohibited: bool = False

class SchemaLoader:
    @staticmethod
    def load(yaml_path: str | Path) -> FeatureSchema:
        # Parses features with feat_data.get() pattern for optional fields
```

From backend/config/features.yaml (one feature example):
```yaml
- name: total_bilirubin
  model_column: "T. Bilirubin"
  type: numeric
  required: false
  strongly_recommended: true
  valid_range:
    min: 0
    max: 100
  unit: "mg/dL"
  description: "..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add display_name, category, and follow_up_question to features.yaml</name>
  <files>backend/config/features.yaml</files>
  <read_first>
    - backend/config/features.yaml
    - .planning/phases/06-reply-builder-polish/06-CONTEXT.md
    - .planning/phases/06-reply-builder-polish/06-RESEARCH.md
  </read_first>
  <action>
Add three new fields to each of the 13 feature entries in features.yaml. Insert them immediately after the `model_column` line for each feature (per D-01, D-03, D-07). Use these exact values:

1. sex:
   display_name: "Sex"
   category: demographics
   follow_up_question: "What is the patient's biological sex (male or female)?"

2. age:
   display_name: "Age"
   category: demographics
   follow_up_question: "How old is the patient?"

3. clinical_cholangitis:
   display_name: "Cholangitis"
   category: clinical_conditions
   follow_up_question: "Does the patient have signs of cholangitis?"

4. clinical_pancreatitis:
   display_name: "Pancreatitis"
   category: clinical_conditions
   follow_up_question: "Is there clinical or lab evidence of pancreatitis?"

5. clinical_cholecystitis:
   display_name: "Cholecystitis"
   category: clinical_conditions
   follow_up_question: "Does the patient have clinical cholecystitis?"

6. ast:
   display_name: "AST"
   category: labs
   follow_up_question: "What is the patient's AST level (U/L)?"

7. alt:
   display_name: "ALT"
   category: labs
   follow_up_question: "What is the patient's ALT level (U/L)?"

8. alkaline_phosphatase:
   display_name: "Alkaline Phosphatase"
   category: labs
   follow_up_question: "Do you have the patient's alkaline phosphatase level (U/L)?"

9. total_bilirubin:
   display_name: "Total Bilirubin"
   category: labs
   follow_up_question: "Do you have the patient's total bilirubin level (mg/dL)?"

10. abdominal_ultrasound_performed:
    display_name: "Abdominal Ultrasound"
    category: imaging
    follow_up_question: "Was an abdominal ultrasound performed?"

11. cbd_stone_on_ultrasound:
    display_name: "CBD Stone on Ultrasound"
    category: imaging
    follow_up_question: "Was a CBD stone seen on the abdominal ultrasound?"

12. cbd_stone_on_ct:
    display_name: "CBD Stone on CT"
    category: imaging
    follow_up_question: "Was a CBD stone seen on CT scan?"

13. charlson_comorbidity_index:
    display_name: "Charlson Comorbidity Index"
    category: clinical_conditions
    follow_up_question: "Do you have the patient's Charlson Comorbidity Index score?"

Preserve all existing fields. Do not modify model_column, type, required, encoding, valid_range, allowed_values, unit, description, strongly_recommended, triggers_fast_path, or inference_prohibited values.
  </action>
  <acceptance_criteria>
    - features.yaml contains exactly 13 occurrences of "display_name:"
    - features.yaml contains exactly 13 occurrences of "category:"  (search for lines starting with whitespace + "category:" to distinguish from the YAML key)
    - features.yaml contains exactly 13 occurrences of "follow_up_question:"
    - `grep 'display_name: "Total Bilirubin"' backend/config/features.yaml` returns a match
    - `grep 'category: labs' backend/config/features.yaml` returns at least 4 matches (ast, alt, alkaline_phosphatase, total_bilirubin)
    - `grep 'category: demographics' backend/config/features.yaml` returns exactly 2 matches (sex, age)
    - `grep 'category: imaging' backend/config/features.yaml` returns exactly 3 matches
    - `grep 'category: clinical_conditions' backend/config/features.yaml` returns exactly 4 matches
    - All existing fields (model_column, type, required, etc.) are unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -c 'display_name:' config/features.yaml && grep -c 'follow_up_question:' config/features.yaml && python -c "from backend.app.core.schema_loader import SchemaLoader; s = SchemaLoader.load('backend/config/features.yaml'); print('features:', len(s.features))"</automated>
  </verify>
  <done>All 13 features in features.yaml have display_name, category, and follow_up_question fields with the exact values specified above.</done>
</task>

<task type="auto">
  <name>Task 2: Extend FeatureDefinition and SchemaLoader to parse new fields</name>
  <files>backend/app/core/schema_loader.py, backend/tests/test_schema_loader.py</files>
  <read_first>
    - backend/app/core/schema_loader.py
    - backend/tests/test_schema_loader.py
    - backend/config/features.yaml
  </read_first>
  <action>
1. In `backend/app/core/schema_loader.py`, add three new optional fields to the `FeatureDefinition` dataclass, after the `inference_prohibited` field:

```python
    display_name: Optional[str] = None
    category: Optional[str] = None
    follow_up_question: Optional[str] = None
```

2. In `SchemaLoader.load()`, add three new `.get()` lines inside the `for feat_data in data["features"]` loop, after the `inference_prohibited` line (line 99):

```python
                display_name=feat_data.get("display_name"),
                category=feat_data.get("category"),
                follow_up_question=feat_data.get("follow_up_question"),
```

3. In `backend/tests/test_schema_loader.py`, add three tests:

```python
def test_display_name_loaded(schema):
    feat = schema.get_feature_by_name("total_bilirubin")
    assert feat.display_name == "Total Bilirubin"

def test_category_loaded(schema):
    feat = schema.get_feature_by_name("total_bilirubin")
    assert feat.category == "labs"

def test_follow_up_question_loaded(schema):
    feat = schema.get_feature_by_name("total_bilirubin")
    assert feat.follow_up_question is not None
    assert "bilirubin" in feat.follow_up_question.lower()
```

These follow the existing test pattern (the `schema` fixture already loads from the real features.yaml).
  </action>
  <acceptance_criteria>
    - `grep 'display_name' backend/app/core/schema_loader.py` returns at least 3 matches (field def, .get(), Optional import already exists)
    - `grep 'category' backend/app/core/schema_loader.py` returns at least 2 matches (field def, .get())
    - `grep 'follow_up_question' backend/app/core/schema_loader.py` returns at least 2 matches
    - `grep 'def test_display_name_loaded' backend/tests/test_schema_loader.py` returns a match
    - `grep 'def test_category_loaded' backend/tests/test_schema_loader.py` returns a match
    - `grep 'def test_follow_up_question_loaded' backend/tests/test_schema_loader.py` returns a match
    - `cd backend && python -m pytest tests/test_schema_loader.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/khalidahusain/Library/Mobile Documents/com~apple~CloudDocs/Work/Hopkins/Programming Projects/CBD-stone-LLM" && python -m pytest backend/tests/test_schema_loader.py -x -v</automated>
  </verify>
  <done>FeatureDefinition has display_name, category, follow_up_question fields; SchemaLoader parses them from YAML; three new tests pass confirming the values are loaded correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file -> SchemaLoader | Configuration file parsed at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | features.yaml | accept | Config file is server-side only, not user-editable at runtime; file permissions control access |
| T-06-02 | I (Info Disclosure) | follow_up_question field | accept | Follow-up questions contain no sensitive data — they are clinician-facing prompts about standard clinical variables |
</threat_model>

<verification>
- All existing tests pass: `python -m pytest backend/tests/ -x`
- SchemaLoader loads all 13 features with new fields populated
- No existing field values changed in features.yaml
</verification>

<success_criteria>
- features.yaml has display_name, category, follow_up_question on all 13 features
- FeatureDefinition dataclass has the three new Optional fields
- SchemaLoader.load() parses them via .get() pattern
- All existing + 3 new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-reply-builder-polish/06-01-SUMMARY.md`
</output>
