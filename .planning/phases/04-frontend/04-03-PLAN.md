---
phase: 04-frontend
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/DashboardPanel.tsx
  - frontend/src/components/dashboard/ProbabilityDisplay.tsx
  - frontend/src/components/dashboard/GuidanceBar.tsx
  - frontend/src/components/dashboard/CostTable.tsx
  - frontend/src/components/dashboard/InterpretationGuide.tsx
  - frontend/src/components/dashboard/Abbreviations.tsx
  - frontend/src/components/dashboard/CholangitisOverlay.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "ProbabilityDisplay shows probability as large percentage with color-coded risk tier label"
    - "GuidanceBar is a CSS gradient with 4 labeled zones and an animated pointer at the current probability"
    - "CostTable shows 4-row table (IOC, MRCP, ERCP, EUS) with probability-weighted costs formatted as currency"
    - "InterpretationGuide is always visible with static text explaining probability ranges"
    - "Abbreviations panel is always visible with clinical term definitions"
    - "CholangitisOverlay shows ASGE 2019 banner when cholangitis_flag is true"
    - "All components visible simultaneously without scrolling on 1080p desktop"
  artifacts:
    - path: "frontend/src/components/DashboardPanel.tsx"
      provides: "Complete dashboard container"
    - path: "frontend/src/components/dashboard/GuidanceBar.tsx"
      provides: "CSS gradient management guidance bar with animated pointer"
    - path: "frontend/src/components/dashboard/CostTable.tsx"
      provides: "Probability-weighted cost intervention table"
---

<objective>
Build all dashboard components: probability display, management guidance bar, cost table, interpretation guide, abbreviations reference, and cholangitis overlay. These compose into DashboardPanel which occupies the right ~1/3 of the screen.

Purpose: The dashboard is the clinical decision support surface — it shows the ML model's output in an interpretable format. All elements must be visible simultaneously on desktop (FRNT-03) so the clinician can see probability, recommended intervention, costs, and reference information at a glance.

Output: Complete DashboardPanel with all sub-components, reading prediction data from ChatContext.
</objective>

<context>
@.planning/phases/04-frontend/04-CONTEXT.md

<interfaces>
From Plan 04-01:
  - ChatContext provides: state.prediction (PredictionResult | null)
  - PredictionResult: probability, risk_tier, recommended_intervention, cost_estimates, cholangitis_flag, cholangitis_message, imputed_fields

From existing Dash app (visual reference):
  - Gradient: rgb(198,239,1) → rgb(0,100,0)
  - Zones: 0-10% CCY±IOC, 10-44% MRCP, 44-90% EUS, 90-100% ERCP
  - Cholangitis: blur gradient bar + overlay red text ASGE message
  - Cost formula: base_cost + variable_cost * probability / 100
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard sub-components</name>
  <files>
    frontend/src/components/dashboard/ProbabilityDisplay.tsx
    frontend/src/components/dashboard/GuidanceBar.tsx
    frontend/src/components/dashboard/CostTable.tsx
    frontend/src/components/dashboard/InterpretationGuide.tsx
    frontend/src/components/dashboard/Abbreviations.tsx
    frontend/src/components/dashboard/CholangitisOverlay.tsx
  </files>
  <read_first>
    - docs/cbd app/assets/utils.py (lines 164-233 — gradient colors, zone boundaries, pointer)
    - docs/cbd app/app.py (lines 493-543 — cholangitis overlay, cost table)
    - docs/cbd app/assets/abbreviation.json (abbreviation definitions)
    - backend/config/features.yaml (risk_tiers, cost_values)
    - .planning/phases/04-frontend/04-CONTEXT.md (D-11 through D-16, D-21)
  </read_first>
  <action>
Create `frontend/src/components/dashboard/ProbabilityDisplay.tsx`:
- Large percentage: "45.0%" in bold, big font
- Risk tier label below: "High Risk" with color coding
- Colors: low=green, intermediate=amber, high=orange, very_high=red
- Empty state: "Awaiting clinical data..." in gray

Create `frontend/src/components/dashboard/GuidanceBar.tsx`:
- Container div with CSS linear-gradient background (rgb(198,239,1) to rgb(0,100,0))
- 4 labeled zone overlays with text: "CCY ± IOC", "MRCP", "EUS", "ERCP"
- Zone boundaries at 10%, 44%, 90% with vertical divider lines
- Pointer: absolutely-positioned triangle/arrow marker at `left: {probability}%`
- CSS `transition: left 0.5s ease` for smooth animation when probability changes
- Tick marks at 0%, 10%, 44%, 90%, 100% below the bar
- Props: probability (number), recommended_intervention (string)

Create `frontend/src/components/dashboard/CostTable.tsx`:
- Simple HTML table with Tailwind styling
- 4 rows: IOC, MRCP, ERCP, EUS
- Columns: Intervention, Expected Cost
- Costs formatted as "$1,234.56"
- Props: cost_estimates (CostEstimate[])

Create `frontend/src/components/dashboard/InterpretationGuide.tsx`:
- Static panel with compact text:
  - "0-10%: Low risk → CCY ± IOC (cholecystectomy with optional intraoperative cholangiography)"
  - "10-44%: Intermediate risk → MRCP (magnetic resonance cholangiopancreatography)"  
  - "44-90%: High risk → EUS (endoscopic ultrasound)"
  - "90-100%: Very high risk → ERCP (endoscopic retrograde cholangiopancreatography)"
- Header: "Interpretation Guide"
- Small font, compact layout

Create `frontend/src/components/dashboard/Abbreviations.tsx`:
- Static panel with abbreviation definitions
- Two-column or list format: "ERCP — Endoscopic retrograde cholangiopancreatography"
- Include: ERCP, EUS, MRCP, IOC, CCY, CBD, ALP, AST, ALT, CCI
- Header: "Abbreviations"
- Small font

Create `frontend/src/components/dashboard/CholangitisOverlay.tsx`:
- Renders when cholangitis_flag is true
- Overlays the GuidanceBar with a blur effect (CSS filter: blur(4px))
- Centered red-text banner: cholangitis_message (ASGE 2019 text)
- White semi-transparent background behind the text
- Props: cholangitis_flag, cholangitis_message
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 6 dashboard sub-components created
    - GuidanceBar uses CSS gradient with 4 labeled zones and animated pointer
    - CostTable formats costs as currency
    - Cholangitis overlay blurs bar and shows ASGE message
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All dashboard sub-components built with Tailwind styling.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardPanel container</name>
  <files>
    frontend/src/components/DashboardPanel.tsx
  </files>
  <read_first>
    - frontend/src/components/dashboard/*.tsx (all sub-components from Task 1)
    - frontend/src/context/ChatContext.tsx (prediction state)
    - .planning/phases/04-frontend/04-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
Create `frontend/src/components/DashboardPanel.tsx`:
- Reads prediction from ChatContext
- Composes all sub-components in vertical stack order:
  1. ProbabilityDisplay
  2. GuidanceBar (with CholangitisOverlay when applicable)
  3. Recommended intervention text
  4. CostTable
  5. InterpretationGuide
  6. Abbreviations
- When prediction is null: show empty state ("Awaiting clinical data...")
- Compact spacing with Tailwind: `space-y-2` or `space-y-3`, small padding
- Overflow-y-auto for smaller viewports
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPanel composes all sub-components
    - Shows empty state when no prediction
    - Shows full dashboard when prediction available
    - Compact layout fits on 1080p without scrolling
  </acceptance_criteria>
  <done>DashboardPanel container wiring all sub-components from Context state.</done>
</task>

</tasks>

<success_criteria>
- All dashboard components render correctly from PredictionResult data
- Guidance bar gradient matches existing Dash app visual style
- All elements fit in ~1/3 screen width on 1080p
- Cholangitis overlay works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend/04-03-SUMMARY.md`
</output>
